# b777_L1 — companion doc

Companion to `examples/B777/inputs/b777_L1.json` (not yet written) — the Boeing 777-200LR
Level-1 **SPEC** data (what the aircraft IS). Every discipline method for this example uses the
metabook Chapter 4 statistical method at "L1" discipline fidelity. Both the L1 and the L2 sizing
algorithms read this one spec file.

**SCOPE GUARD.** This file holds SPEC data only — reference areas, aspect ratio, taper, fuselage
size, engine thrust, engine model, config-polar table. It never holds REQUIREMENTS (what the
aircraft must DO — field lengths, climb gradients, cruise point, range: those stay in
`examples/B777/inputs/b777_requirements.json`). The `aircraft_category` sits at the top level, not
inside a discipline block.

**Provenance.** The metabook worked Example 4.2 is the ground-truth source; it is a SECONDARY
source that cites Raymer / Roskam / Mattingly. Citations below give the metabook Eq/Table and the
primary table the metabook itself names. `[metabook §4.11]` = the worked Example 4.2 text,
`docs/reference_extracts/metabook_data.md`.

**Discrepancy note.** The metabook wetted-area exponent (`d = 0.7531`) disagrees with the repo's
current `GeomL1.lookup_swet` transport_jet row (`d = 0.7351`). This is logged as discrepancy **D3**
in `metabook_data.md` "Known discrepancies", RESOLVED (Sarojini 2026-08-13) in favour of the
metabook value; the repo row is to be corrected in the src-extension batch. The B777 example uses
`d = 0.7531`.

---

## 1. Top-level key

| Key | Value | Consumers | Source |
|-----|-------|-----------|--------|
| `aircraft_category` | `"jet_transport"` | weights (Raymer Table 3.1 row), geometry (`transport_jet` swet/lfus rows), CLmax table row | metabook §4.11 (777-200LR, long-range airliner) |

**Table-row-name note.** The canonical key is `jet_transport` (the Raymer Table 3.1 row name used
by `WeightsL1.lookup_coeffs`). Two other repo tables spell the same category `transport_jet`
(`GeomL1.lookup_swet`, `GeomL1.lookup_lfus`). Each lookup translates the canonical value to its own
table's printed row name — do not rename a row to match the key.

---

## 2. Geometry block

Read by `B777GeomL2` (real trapezoidal planform). The `S_ref`/`AR`/`S_wet_rest`/`L_fus` rows below
are the L1 core; `B777GeomL2` ADDS the metabook Table 7.2 equivalent-trapezoidal parameters
(`wing_trapezoid` / `htail_trapezoid` / `vtail_trapezoid`) and the `fuselage` block, from which it
computes the MAC (Eq. 7.2–7.9) and the **exposed** planform / fuselage wetted areas (metabook Table
7.3: wing 3923, HT 903, VT 604, fuselage 13125 ft²). Full details + the exposed-area method:
`models/disciplines/geom/B777GeomL2.md` and `docs/reference_extracts/metabook_data.md` §7.2.

| Property | Value | Unit | Kind | Citation |
|---|---|---|---|---|
| `S_ref` | 4605 | ft² | input | [metabook Table 4.3] |
| `AR` | 9.8 | — | input | [metabook Table 4.3] |
| `n_engines` | 2 | — | input | [metabook §4.11] (2× GE90-110B) |
| `S_wet_rest` | 19081 | ft² | input | [metabook Eq. 4.58 decomposition of Eq. 4.42] — see arithmetic below |
| `L_fus` | 209 | ft | input `_TODO` | stand-in ≈ 209 ft / 63.7 m; needs a primary source |
| `wing_taper` | — | — | decision | see §2.3 — recommend NOT carrying it |

### 2.1 `S_wet_rest` arithmetic (show the decomposition)

The metabook Eq. 4.58 splits total wetted area into a wing part (approximated as `2·S`) and the
rest of the aircraft:

```
Swet      = 10^0.0199 * 766,800^0.7531 = 28,291 ft²          (metabook Eq. 4.42)
Swet      = Swet_rest + 2*S              (metabook Eq. 4.58)
Swet_rest = 28,291 - 2*4,605 = 28,291 - 9,210 = 19,081 ft²
```

`S_wet_rest = 19,081 ft²` is the wetted area of everything except the wing. It is carried as a spec
input so the L2 sizing algorithm can recompute `CD0(S)` when it varies the wing area S (Eq. 4.58):
`CD0 = Cf * (S_wet_rest + 2*S) / S`. The full-airplane MTOW-regressed `Swet = 28,291 ft²` from
Eq. 4.42 is a DERIVED cross-check, not a stored input — it is regenerated from the Eq. 4.9
regression at the baseline W0.

**Citation caveat.** The `766,800` in Eq. 4.42 is the baseline MTOW (Table 4.3); the `S = 4605`
divisor is `S_ref` above. Both are cited. The exponent `0.7531` is the metabook value (D3), not the
current repo `0.7351`.

### 2.2 `L_fus` — `_TODO`

- Stand-in value: `209 ft` (≈ 63.73 m), the published Boeing 777-200LR overall length.
- Primary source needed: a Boeing 777-200LR type document / Airport Planning document giving the
  overall length. The metabook Example 4.2 does not print a fuselage or overall length.
- Consumer: an L1 fuselage-based wetted-area or tail-arm path, if the B777 uses one. Note the
  metabook Example 4.2 itself does NOT use `L_fus` — it takes `Swet` straight from the Eq. 4.9
  MTOW regression (Eq. 4.42), so `L_fus` may end up unused at L1. Carried as a stand-in for
  completeness and for the tail-sizing path (§2.4).

### 2.3 Wing taper — decision, not carried (recommendation)

The metabook Example 4.2 never uses the wing taper ratio. It builds the drag polars from `AR`, `e`,
and the config `ΔCD0` values only, and it takes `Swet` from the MTOW regression, not from planform
chords. So MAC is not needed for any L1 equation in this example.

- **Recommendation: do NOT carry `wing_taper` at L1.** Where a mean chord is genuinely needed (e.g.
  a future tail-volume path), use the standard mean chord `cbar = S/b` [metabook Eq. 11.5 note,
  `c_bar = S/b`], which needs only `S_ref` and `AR` (both already inputs: `b = sqrt(AR*S_ref)`).
- If a later fidelity step needs the true trapezoidal MAC, add `wing_taper` then with a cited
  777-200LR value (which is itself a `_TODO` — no taper appears in the metabook). Recording the
  choice here avoids carrying an uncited taper that no equation reads.

### 2.4 Tail sizing — new row needed later (flag)

The metabook Chapter 8 gives jet-transport tail volume coefficients `cVT ≈ 0.09`, `cHT ≈ 1.0`
[metabook Eqs. 8.1/8.2]. The repo `TailL1.lookup_tail_volume_coeffs` currently implements the
`jet_fighter` row ONLY and errors on any other category. A `jet_transport` row (`c_HT = 1.0`,
`c_VT = 0.09`) must be ADDED to `TailL1` in the implementation phase before the B777 can size a
tail through that toolbox. This is a code change, not a JSON input — flagged here so it is not
missed. No tail block is required in `b777_L1.json` for the Example 4.2 sizing itself (tail is not
exercised by Eqs. 4.42–4.57).

---

## 3. Aerodynamics block

Read by the future `B777AeroL1`. The metabook builds five configuration drag polars plus an
approach polar (Eqs. 4.42–4.44 and the five-polar table, §4.11). All five reproduce exactly from
`CD0` and `K` per configuration.

| Property | Value | Unit | Kind | Citation |
|---|---|---|---|---|
| `Cfe` | 0.0026 | — | input | [Raymer Table 12.3, civil transport; metabook Eq. 4.43] |
| `e_clean` | 0.85 | — | input | [metabook Table 4.2 clean; §4.11] |

### 3.1 The config-increment table (reproduces the five metabook polars)

Each row carries the config's total `CD0` and its induced factor `K = 1/(pi*AR*e)` with `AR = 9.8`.
The values below are the printed polars (metabook §4.11, five-polar table), so they reproduce
Eqs. 4.49–4.57 exactly. `K1` in the repo drag-polar convention `CD = CD0 + K1*CL² + K2*CL` equals
the metabook's `1/(pi*AR*e)` (K2 = 0 — the metabook high-lift polars are uncambered-basis).

| config string | CD0 | K1 (= 1/(π·AR·e)) | e implied | CLmax | Citation |
|---|---|---|---|---|---|
| `clean` | 0.01597 | 0.03815 | 0.851 | 0.9 | [metabook Eqs. 4.44 / five-polar table]; CLmax [Roskam Table 3.1] |
| `takeoff_flaps_gear_up` | 0.03597 | 0.04054 | 0.801 | 2.0 | [metabook five-polar table]; CLmax [Roskam Table 3.1] |
| `takeoff_flaps_gear_down` | 0.06097 | 0.04054 | 0.801 | 2.0 | [metabook five-polar table]; CLmax [Roskam Table 3.1] |
| `landing_flaps_gear_up` | 0.09097 | 0.04324 | 0.751 | 2.6 | [metabook five-polar table]; CLmax [Roskam Table 3.1] |
| `landing_flaps_gear_down` | 0.11597 | 0.04324 | 0.751 | 2.6 | [metabook five-polar table]; CLmax [Roskam Table 3.1] |
| `approach` | 0.08847 | 0.04324 | 0.751 | 2.21 | [metabook §4.11 Climb 6 note] — see §3.3 |

The six config strings match the set `AerodynamicsBase.get_config_polar` validates, exactly:
`clean, takeoff_flaps_gear_up, takeoff_flaps_gear_down, landing_flaps_gear_up,
landing_flaps_gear_down, approach`. `B777AeroL1` must implement `get_config_polar(config)`
returning `struct(CD0, K1, K2, CLmax)` for each, so the FAR-25 field-length and climb-gradient
constraints can read them through the bridge.

### 3.2 CLmax by config

| Quantity | Value | Citation |
|---|---|---|
| CLmax clean | 0.9 | [Roskam Table 3.1; metabook §4.11] |
| CLmax takeoff | 2.0 | [Roskam Table 3.1; metabook §4.11] |
| CLmax landing | 2.6 | [Roskam Table 3.1; metabook §4.11] |

**D1 / D2 flag (affects the climb configs, not the physical CLmax).** The metabook printed climb
Eqs. 4.49–4.51 use `CLmax = 2.2` in the takeoff-config CLmax slot, not the `2.0` stated on p. 45,
and Eq. 4.49 uses the gear-UP `CD0 = 0.03597` for a gear-down FAR segment. Both are metabook-internal
discrepancies, logged D1/D2 in `metabook_data.md`, RESOLVED (Sarojini 2026-08-13) in favour of the
PRINTED values for worked-example parity. The config-CLmax basis this creates is a gate decision —
see `B777_decisions.md` §2. The table above carries the PHYSICAL CLmax (2.0 TO / 2.6 landing) so
the takeoff/landing FIELD-LENGTH constraints reproduce Eqs. 4.46/4.48; the climb-row parity handling
is the decision to be confirmed.

### 3.3 Approach polar (derived from the takeoff/landing gear-down rows)

The metabook approximates the balked-landing (Climb 6) approach configuration as the mean of the
takeoff and landing gear-down CD0, and approach CLmax as 0.85 of landing CLmax [metabook §4.11
Climb 6 note]:

```
CD0_approach   = mean(0.06097, 0.11597) = 0.08847
CLmax_approach = 0.85 * 2.6 = 2.21
K1_approach    = 0.04324    (landing-flaps induced factor)
```

`CD0_approach = 0.08847` and `CLmax_approach = 2.21` are DERIVED from the table above, not
independent inputs. Carry them either as a computed `approach` row in `B777AeroL1.get_config_polar`
or as an explicit table row; recommend computing them so a change to the takeoff/landing rows
propagates.

---

## 4. Propulsion block

Read by the future `B777PropL1`. Engine is 2× GE90-110B.

| Property | Value | Unit | Kind | Citation |
|---|---|---|---|---|
| `engine_type` | `"high_bypass_turbofan"` | — | input | [metabook §4.11]; selects `PropL1` lapse exponent + TSFC row |
| `n_engines` | 2 | — | input | [metabook §4.11] |
| `T_SL` | 220000 | lbf | input | total, 2× GE90-110B; per-engine rating is `_TODO` — see §4.1 |
| `lapse_exponent_m` | 0.6 (recommend) OR ~1.0 | — | input, gate | see `B777_decisions.md` §1 (D5) |
| `TSFC_model` | Mattingly high-BPR | — | method | `c = (0.4 + 0.45*M)*sqrt(theta)` [metabook Eq. 10.11] |

### 4.1 `T_SL` and the per-engine rating — `_TODO`

- Total `T_SL = 220,000 lbf` is the value the metabook T–S plot marks for the actual 777-200LR
  ("Actual 777-200LR (T = 220000 lb and S = 4605 ft²)", metabook Fig. 4.7 caption). This total is
  CITED to the metabook.
- The per-engine `-110B` rating (≈ 110,100 lbf/engine) is `_TODO`: needs a primary GE / Boeing
  source. The metabook GE90 Table 10.1 prints the GE90 SLS range as 76,000 lb (777-200) to
  115,000 lb (777-300ER), which brackets 110,000 but does not pin the specific -110B rating.
  Stand-in: 110,000 lbf/engine.
- Note the internal consistency check: `2 × 110,000 = 220,000 lbf`, matching the cited total. Carry
  `T_SL = 220000` (total) as the cited input; carry the per-engine number only if a class needs it,
  marked `_TODO`.

### 4.2 Thrust lapse — `m` is a gate decision (D5)

The metabook Example 4.2 uses `alpha = sigma^0.6` (Eqs. 4.55–4.57), the GENERIC Eq. 10.9 fit with
`m = 0.6`, with no engine-type justification. The metabook's OWN GE90 Table 10.1 data (the real 777
engine) fits `m ≈ 1.0–1.1` for a high-BPR turbofan. `PropL1.lookup_lapse_exponent` currently
returns `m = 0.6` for `high_bypass_turbofan`. Making `m` a cited JSON input (`lapse_exponent_m`)
lets the choice be explicit. Recommendation and the two options are in `B777_decisions.md` §1.

`PropL1.get_thrust_lapse` computes `alpha = sigma^m` with `sigma = rho/rho_SL` from the
`AircraftState` — so the B777 model uses ISA density from the state, not the metabook's printed
off-ISA density ratios (0.2846 at "40,000 ft", 0.2331 at "42,000 ft"). Those printed ratios are used
ONLY by the comparison report, for parity with the printed equations (see D5).

### 4.3 TSFC — Mattingly high-BPR

The metabook Eq. 10.11 high-BPR turbofan form:

```
c = (0.4 + 0.45*M) * sqrt(theta)     [1/hr]     (metabook Eq. 10.11 = Mattingly 1996 Eq. 1.36a)
```

`theta = T_altitude / T_SL` (temperature ratio, from `AircraftState`). This is a METHOD, not a
stored number. The categorical `PropL1.get_TSFC` (0.50 cruise / 0.40 loiter for
`high_bypass_turbofan`, [Raymer Table 3.3]) is the crude L1 fallback; the B777 mission analysis
should prefer the Mattingly Eq. 10.11 Mach/theta-dependent form. The GE90 SLS SFC range 0.29–0.31
[metabook Table 10.1] is a cross-check target, not an input.

---

## 5. Weights block

Read by `B777WeightsL2`, the metabook Chapter 7 **component build-up** (Algorithm 5, Example 7.1):
`OEW = 1.3·n·Wengine(T0) + 10·S_exp_wing + 5.5·S_exp_ht + 5.5·S_exp_vt + 5·S_wet_fus + 0.043·W0 +
0.17·W0`, responding to W0, S_ref (exposed wing) and T0 (engine). Full details:
`models/disciplines/weights/B777WeightsL2.md`. The component build-up needs no empty-weight fraction
and no delta baseline, so of the rows below it uses only the areal densities / fractions (toolbox
constants) and `W_payload`; the `A/C/Kvs` empty-weight-fraction and `W0_baseline`/`S_baseline`/
`T_baseline_per_engine` rows were the deleted `B777WeightsL1` delta-model inputs and are no longer
read (kept in the JSON as harmless legacy keys).

| Property | Value | Unit | Kind | Citation |
|---|---|---|---|---|
| Empty-weight-fraction A | 1.02 | — | input (toolbox const) | [Raymer Table 3.1 jet_transport; metabook §2] |
| Empty-weight-fraction C | −0.06 | — | input (toolbox const) | [Raymer Table 3.1 jet_transport; metabook §2] |
| `Kvs` | 1.00 | — | input (toolbox const) | fixed sweep [metabook §2] |
| areal density, wing | 10 | lb/ft² | input (toolbox const) | [Raymer Table 15.2 transport/bomber; metabook Ch. 7] |
| areal density, H-tail | 5.5 | lb/ft² | input (toolbox const) | [Raymer Table 15.2 transport/bomber; metabook Ch. 7] |
| areal density, V-tail | 5.5 | lb/ft² | input (toolbox const) | [Raymer Table 15.2 transport/bomber; metabook Ch. 7] |
| areal density, fuselage | 5.0 | lb/ft² | input (toolbox const) | [Raymer Table 15.2 transport/bomber; metabook Ch. 7] |
| landing-gear fraction | 0.043 | — | input (toolbox const) | [Raymer Table 15.2 fraction-based, transport; metabook Ch. 7] |
| all-else-empty fraction | 0.17 | — | input (toolbox const) | [metabook Ch. 7 fraction table] |
| `W0_baseline` | 766800 | lbf | input | design-point MTOW [metabook Table 4.3 / Fig. 4.7] |
| `S_baseline` | 4605 | ft² | input | design-point wing area [metabook Table 4.3] |
| `T_baseline_per_engine` | 110000 | lbf | input `_TODO` | design-point per-engine thrust — see §4.1 |
| `W_payload` | — | lbf | input `_TODO` | design payload at the range point — see §5.3 |

### 5.1 Raymer Table 3.1 jet-transport row — already in the repo

`WeightsL1.lookup_coeffs` ALREADY carries the `jet_transport` row: `A = 1.02, C = -0.06,
Kvs = 1.00` [Raymer Table 3.1; `metabook_data.md:24`]. So `We/W0 = 1.02 * W0^(-0.06)` is available
to `B777WeightsL1` with no new toolbox row. These are toolbox Constants selected by
`aircraft_category`, NOT JSON inputs — the JSON carries only `aircraft_category = "jet_transport"`.

### 5.2 Areal densities and engine-weight regression

The Raymer Table 15.2 transport/bomber densities (wing 10, H-tail 5.5, V-tail 5.5, fuselage 5.0
lb/ft²) and the fraction-based rows (landing gear 0.043·W0, all-else-empty 0.17·W0) are all in
`metabook_data.md` Ch. 7 (Raymer Table 15.2 and the fraction table). They are toolbox Constants
selected by `aircraft_category`, matching how the F-16A L2 weights class reads its own densities.

Engine weight uses the Roskam regression form [metabook Ch. 7, Eqs. 7.13–7.19]:

```
Weng_dry     = 0.521 * T0^0.9
Weng_oil     = 0.082 * T0^0.65
Weng_rev     = 0.034 * T0
Weng_control = 0.26  * T0^0.5
Weng_start   = 9.33  * (Weng_dry/1000)^1.078
Wengine      = sum of the above          (metabook Eq. 7.19)
```

`T0` = per-engine SLS thrust [lbf]. These coefficients are toolbox Constants [Roskam; metabook
Ch. 7], not JSON inputs. Cross-check target: the GE90 uninstalled weight 17,300 lb [metabook
Table 10.1].

### 5.3 Baseline design point (delta-weight model) and payload

The metabook §4.12.1 Algorithm 2 sizes empty weight as a delta around a baseline design point:

```
We = (We/W0)*W0 + Dens_wing*(S - S_baseline) + [Weng(T0) - Weng(T0_baseline)]   (metabook Algorithm 2)
```

Baseline design point (the "Actual 777-200LR" marker, metabook Fig. 4.7 caption + Table 4.3):
`W0_baseline = 766,800 lbf`, `S_baseline = 4605 ft²`, `T_baseline` total = 220,000 lbf
(110,000/engine, per-engine rating `_TODO` per §4.1). These are cited to the metabook.

`W_payload` — `_TODO`: the design payload at the range point. The metabook Example 4.2 does not
print a payload. Stand-in: a 777-200LR design payload figure (order ~100,000 lb for a full
passenger + baggage load at the design range). Primary source needed: a Boeing 777-200LR
payload-range chart / Airport Planning document. Marked `_TODO` — do not fabricate a precise value.

---

## 6. `_TODO` summary (uncited values, stand-ins carried)

| Value | Block | Stand-in | Primary source needed |
|---|---|---|---|
| `L_fus` | geometry | 209 ft | Boeing 777-200LR length (type doc / Airport Planning) |
| GE90-110B per-engine rating | propulsion / weights | 110,000 lbf | GE / Boeing engine rating document |
| `W_payload` | weights | ~100,000 lb (order) | 777-200LR payload-range chart |
| `lapse_exponent_m` | propulsion | 0.6 (recommend) | gate decision D5 — not a document gap, a modelling choice |
| `wing_taper` | geometry | not carried | only if MAC-from-taper is needed later (recommend `cbar = S/b` instead) |

All other values in §§1–5 are CITED to the metabook (Eq/Table) or the primary table the metabook
names. The `d = 0.7531` wetted-area exponent carries the D3 discrepancy note.
